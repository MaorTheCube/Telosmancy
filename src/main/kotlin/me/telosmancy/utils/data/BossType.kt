package me.telosmancy.utils.data

/**
 * Boss type classification.
 */
enum class BossType {
    WORLD,      // World bosses (spawn in overworld)
    DUNGEON     // Dungeon bosses (spawn in dungeons)
}
